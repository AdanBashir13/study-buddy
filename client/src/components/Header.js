import React from 'react';
import NavBar from './Navbar';

const Header = () => {
    return (
        <header>
            <h1>Study Buddy</h1>
            <NavBar />
        </header>
    );
};

export default Header;